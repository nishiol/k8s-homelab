apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: torrserver
  namespace: torrserver-earth
spec:
  dependsOn:
    - name: clusterissuer
      namespace: default
    - name: longhorn
      namespace: longhorn-system
  interval: 30m
  releaseName: torrserver
  targetNamespace: torrserver-earth
  storageNamespace: torrserver-earth
  chart:
    spec:
      chart: ./charts/torrserver
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 10m
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    existingSecret: torrserver-earth-credentials
    hostName: earth.ts.nishiol.ru
    certIssuer: prod-issuer
    incomingConnectionsPort: 56335
    nodeSelector:
      cpu: slow
